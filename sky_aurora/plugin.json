{
  "tesserae_compat": "1.x",
  "name": "Sky, Aurora Forecast",
  "version": "0.1.4",
  "kind": "widget",
  "description": "Current planetary Kp index + 3-day forecast from NOAA SWPC. Higher Kp = aurora visible further from the poles. Includes a viewer-latitude hint so the band reads as 'visible from your location' when conditions allow.",
  "icon": "ph-mountains",
  "supports": { "sizes": ["sm", "md", "lg"] },
  "cell_options": [
    { "name": "latitude", "type": "number", "label": "Your latitude (for visibility hint)", "default": "" }
  ],
  "render": { "dither": "none", "needs_network": true }
}
